<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bjsxt.mapper.FlowerMapper1">

    <select id="selectAll" resultType="flower">

        SELECT   *  from   flower

    </select>

    <select id="selectOne" resultType="flower">

        SELECT  * from  flower   where  id =1
    </select>

    <!--Map<String,Object>, map 是java.util.Map的map,mybatis默认已经有内置别名-->
    <select id="selectMap" resultType="map">

        SELECT  *  from  flower

    </select>

</mapper>